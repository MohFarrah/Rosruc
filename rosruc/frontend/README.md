# Frontend

This Vite React app is the demo workbench for creating support tickets and inspecting the placeholder agent response.

## Run

```sh
npm install
npm run dev
```

Set `VITE_API_URL` when the backend is not available at `http://localhost:4000`.
