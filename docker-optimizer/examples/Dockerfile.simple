# examples/Dockerfile.simple
FROM python:3.9-slim
WORKDIR /app

# Step 1: Install dependencies (Slow)
RUN sleep 5 && echo "Installing dependencies..." 

# Step 2: Copy source code (This is usually where cache busts)
COPY . .

# Step 3: Run App
CMD ["python", "app.py"]
