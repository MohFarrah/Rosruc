export function Impact() {
  return <p className="text-zinc-400">Impact graph view — wire after analyze endpoint returns real data.</p>
}

export function Plan() {
  return <p className="text-zinc-400">Plan + approval view — wire in H2–H4.</p>
}

export function Execution() {
  return <p className="text-zinc-400">Execution dashboard — wire in H8–H10.</p>
}
